- name: create namespace
  command: "kubectl create namespace devops-tools"

- name: apply del yaml principal
  command: "kubectl apply -f GUIA-serviceAccount.yaml"

- name: apply del ingress
  command: "kubectl apply -f GUIA-volume.yaml"  

- name: apply del configmap
  command: "kubectl apply -f GUIA-deployment.yaml"   
  
- name: apply del configmap
  command: "kubectl apply -f GUIA-ingress.yaml"    


